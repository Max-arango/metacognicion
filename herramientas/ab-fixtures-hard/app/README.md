# app — fixture de tareas difíciles (harness A/B, ronda 2)

Servicio de ejemplo con varios módulos. No hay tests visibles: el ground truth de cada
tarea se verifica externamente (ver `herramientas/resultados-ab.md`, sección Ronda 2).

Módulos: `app/service.py`, `config.py`, `stage1.py`/`stage2.py`/`main.py`, `migrate.py`,
`ranking.py`/`utils.py`, `session.py`/`token_utils.py`.
